version: 2

before:
  hooks:
    - go mod download

builds:
  # With Tailscale
  - env:
      - CGO_ENABLED=0
    id: "Remarkable Paper Pro"
    main: .
    binary: goMarkableStream
    goos:
      - linux
    goarch:
      - arm64
    tags:
      - tailscale

  - env:
      - CGO_ENABLED=0
    id: "Remarkable 2"
    main: .
    binary: goMarkableStream
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"
    tags:
      - tailscale

  # Without Tailscale (lighter binary)
  - env:
      - CGO_ENABLED=0
    id: "Remarkable Paper Pro - No Tailscale"
    main: .
    binary: goMarkableStream-lite
    goos:
      - linux
    goarch:
      - arm64

  - env:
      - CGO_ENABLED=0
    id: "Remarkable 2 - No Tailscale"
    main: .
    binary: goMarkableStream-lite
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"

archives:
  - name_template: "{{ .Binary }}_{{ .Os }}_{{ .Arch }}"
    wrap_in_directory: true

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: owulveryck
    name: gomarkablestream
